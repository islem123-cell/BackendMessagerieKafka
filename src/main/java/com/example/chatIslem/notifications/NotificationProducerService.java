package com.example.chatIslem.notifications;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.kafka.core.KafkaTemplate;
import org.springframework.stereotype.Service;

import com.example.chatIslem.kafka.NotificationKafka;

@Service
public class NotificationProducerService {

    @Autowired
    private KafkaTemplate<String, NotificationKafka> kafkaTemplate;

    public void sendNotification(NotificationKafka notification) {
        kafkaTemplate.send("Notifications", notification);
    }
}

*/