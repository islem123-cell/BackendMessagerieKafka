package com.example.chatIslem.notifications;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.example.chatIslem.kafka.NotificationKafka;

@Service
public class NotificationService {
    private final NotificationProducerService producerService;

    @Autowired
    public NotificationService(NotificationProducerService producerService) {
        this.producerService = producerService;
    }

    public void sendNotification(String userId, String message) {
        NotificationKafka notification = new NotificationKafka(userId, message);
        producerService.sendNotification(notification);
    }
}
*/